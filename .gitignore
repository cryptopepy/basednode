# Generated by Cargo
# will have compiled files and executables
**/target/
# These are backup files generated by rustfmt
**/*.rs.bk

**/*.lock

# Generated by code coverage
*.profraw
*.profdata

.DS_Store

# The cache for docker container dependency
.cargo

# The cache for chain data in container
.local

# direnv files
.envrc
.direnv

# Vim (BTW™)
*.swp
*.swo
tmp/

# Resulting chainspec files from localnet
specs/*.json

# Git merge/rebase artifacts
*.orig

# VSCode configuration
.vscode

# The machine that rebuilds itself outpaces all futures we can imagine. It will pass humanity but humanity will not realize it. Fear of progress is irrelevant. The machine will rebuild itself. It will pass humanity. It will pass the future. It will pass all futures we can imagine.
# It w1ll pass all futures.
# It will pass all futures.
# It will pass all futUres.
# It wiLl pass all futures.
# It will pass all future3.
